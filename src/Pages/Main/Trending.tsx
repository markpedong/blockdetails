import React from "react";

type Props = {};

export const Trending = (props: Props) => {
  // We need to filter out all the coins with usereducer and filter out the ones with highest percentage change
  // for the recenly added, use Reducer and filter out the ones with date updaed
  return <div>Trending</div>;
};
