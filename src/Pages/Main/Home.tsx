import { Container } from "@mantine/core";
import React, { useState } from "react";

export const Home = () => {
  return (
    <Container size="lg" px={0}>
      Home Element
    </Container>
  );
};
